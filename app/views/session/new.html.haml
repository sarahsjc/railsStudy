.row-fluid.search-form
  = form_tag '/session', method: :post do
    .label
      username
      = text_area_tag  'username', @login
    .span6(style="margin-top:40px;margin-bottom:40px;")
      password
      = text_area_tag 'password', @password
      %br
      = submit_tag 'login', class: 'btn btn-success'

